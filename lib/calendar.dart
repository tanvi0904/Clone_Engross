import 'package:flutter/material.dart';

class CalendarScreen extends StatelessWidget {
  final Color color;

  CalendarScreen(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}
